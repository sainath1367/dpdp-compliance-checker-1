services:

  backend:
    build:
      context: ./dpdp-backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./dpdp-frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"