services:

  backend:
    build:
      context: ./dpdp-backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./dpdp-frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"